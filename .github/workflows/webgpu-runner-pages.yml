name: WebGPU Runner Pages

on:
  workflow_run:
    workflows: ["WebGPU Runner"]
    types: [completed]
  pull_request:
    types: [closed]

permissions:
  actions: read
  contents: write
  pull-requests: write

concurrency:
  group: pages-${{ github.event.workflow_run.id || github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  publish:
    uses: dioxuslabs/dioxus-ci/.github/workflows/pages-publish.yml@main
    with:
      runs-on: ubuntu-24.04
