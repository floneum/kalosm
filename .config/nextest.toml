# Warn after two minutes. Software-GPU training and model downloads need
# more than four minutes on shared CI runners; retain a finite stall limit.
[profile.default]
slow-timeout = { period = "120s", terminate-after = 10 }
fail-fast = false
